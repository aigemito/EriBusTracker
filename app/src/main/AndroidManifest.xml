<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.emito.eribus">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.RouteDetailActivity"></activity>
        <activity android:name=".activity.RouteDetailMapActivity" />
        <activity android:name=".activity.CustomerActivity" />
        <activity android:name=".login.UserDetailListActivity" />
        <activity
            android:name=".activity.AdministratorActivity"
            android:label="@string/title_activity_administrator"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.DriverActivity" />
        <activity android:name=".ForgotPassword" />
        <activity android:name=".createAccount" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity">

            <!-- <activity android:name=".ul"> -->
            <!-- <activity android:name=".userList_detail_activity"> -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>